---
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: jenkins-rs
  labels:
    purpose: rs-demo
spec:
  replicas: 3
  minReadySeconds: 0
  selector:
    matchLabels:
      app: jenkins
      release: latest
  template:
    metadata:
      name: jenkins-rs
      labels:
        app: jenkins
        release: latest
    spec:
      containers:
        - image: jenkins/jenkins:lts-jdk17
          name: jenkins
          ports:
            - containerPort: 8080
              protocol: TCP

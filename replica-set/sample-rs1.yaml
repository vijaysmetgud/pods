---
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: jenkins
  labels:
    purpose: rs-demo
spec:
  replicas: 3
  minReadySeconds: 0
  selector:
    matchLabels:
      app: jenkins
  template:
    metadata:
      name: jenkins
      labels:
        app: jenkins
    spec:
      containers:
        - image: jenkins/jenkins:jdk17
          name: jenkins
          ports:
            - containerPort: 8080
              protocol: TCP
